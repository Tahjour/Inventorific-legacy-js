import styles from "./main-bar.module.css"

function MainBar() {
    return (
        <section className={styles.mainBar}>
            <h1>This is the main bar section</h1>
        </section>
    );
}

export default MainBar;