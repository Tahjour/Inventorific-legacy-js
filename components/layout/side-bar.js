import styles from "./side-bar.module.css"

function SideBar() {
    return (
        <section className={styles.sideBar}>
            <p>This is the side bar section</p>
        </section>
    );
}

export default SideBar;